apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "matilda-chart.fullname" . }}-test-connection"
  labels:
    {{- include "matilda-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "matilda-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
