apiVersion: v1
kind: Service
metadata:
  name: {{ include "matilda-chart.fullname" . }}
  labels:
    {{- include "matilda-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "matilda-chart.selectorLabels" . | nindent 4 }}
